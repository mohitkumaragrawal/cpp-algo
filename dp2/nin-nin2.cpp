#include <iostream>
#include <vector>

using namespace std;

int main() {
  int T;
  cin >> T;
  while (T--) {
    int N;
    cin >> N;

    vector<vector<int>> points(N, vector<int>(3));
    for (int i = 0; i < N; ++i) {
      for (int j = 0; j < 3; ++j) {
        cin >> points[i][j];
      }}

      
  }
}
