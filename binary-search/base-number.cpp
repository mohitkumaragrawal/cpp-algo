#include <cmath>
#include <iostream>

using namespace std;

int main() {
  freopen("base-number.txt", "r", stdin);
  int T = 0;
  while (!cin.eof()) {
    int a, n, b;
    T++;
    cin >> a >> n >> b;
  }
  cout << T << endl;
}